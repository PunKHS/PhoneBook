DROP TABLE IF EXISTS EMPLOYEE;
DROP TABLE IF EXISTS PROFESSION;
DROP TABLE IF EXISTS PERSON;
DROP TABLE IF EXISTS DEPARTMENT;
DROP TABLE IF EXISTS USER_ROLES;
DROP TABLE IF EXISTS USERS;
DROP TABLE IF EXISTS ROLES;

--DEPARTMENT TABLE
CREATE TABLE IF NOT EXISTS DEPARTMENT (
  ID   INTEGER GENERATED BY DEFAULT AS IDENTITY ( START WITH 100, INCREMENT BY 1) NOT NULL,
  NAME VARCHAR(200),
  HEAD INTEGER,
  PRIMARY KEY (ID)
);

--PERSON TABLE
CREATE TABLE IF NOT EXISTS PERSON (
  ID        INTEGER GENERATED BY DEFAULT AS IDENTITY ( START WITH 100, INCREMENT BY 1) NOT NULL,
  NAME      VARCHAR(200),
  AVAILABLE INTEGER,
  PRIMARY KEY (ID)
);

--PROFESSION TABLE
CREATE TABLE IF NOT EXISTS PROFESSION (
  ID   INTEGER GENERATED BY DEFAULT AS IDENTITY ( START WITH 100, INCREMENT BY 1) NOT NULL,
  NAME VARCHAR(200),
  PRIMARY KEY (ID)
);

--EMPLOYEE TABLE
CREATE TABLE IF NOT EXISTS EMPLOYEE (
  ID            INTEGER GENERATED BY DEFAULT AS IDENTITY ( START WITH 1, INCREMENT BY 1) NOT NULL,
  PERSON_ID     INTEGER,
  PROFESSION_ID INTEGER,
  DEPARTMENT_ID INTEGER,
  EMAIL         VARCHAR(30),
  PHONE         VARCHAR(20),
  MOBILE_PHONE  VARCHAR(20),
  AVAILABLE     INTEGER,
  PRIMARY KEY (ID)
);

--USERS TABLE
CREATE TABLE IF NOT EXISTS USER (
  ID       INTEGER GENERATED BY DEFAULT AS IDENTITY ( START WITH 1, INCREMENT BY 1) NOT NULL,
  USERNAME VARCHAR(200),
  PASSWORD VARCHAR(200),
  PRIMARY KEY (ID)
);

--ROLES TABLE
CREATE TABLE IF NOT EXISTS ROLE (
  ID   INTEGER GENERATED BY DEFAULT AS IDENTITY ( START WITH 1, INCREMENT BY 1) NOT NULL,
  NAME VARCHAR(50),
  PRIMARY KEY (ID)
);

--MAPPING USERS AND ROLES TABLE
CREATE TABLE IF NOT EXISTS USER_ROLES (
  USER_ID INTEGER,
  ROLE_ID INTEGER,

  FOREIGN KEY (USER_ID) REFERENCES USER (ID) ON DELETE CASCADE ON UPDATE CASCADE,
  FOREIGN KEY (ROLE_ID) REFERENCES ROLE (ID) ON DELETE CASCADE ON UPDATE CASCADE,

  UNIQUE (USER_ID, ROLE_ID)
);

--FOREIGN KEYS
--ALTER TABLE USER_ROLES ADD CONSTRAINT FK_USER FOREIGN KEY (USER_ID) REFERENCES USERS (ID) ON DELETE CASCADE ON UPDATE CASCADE;
--ALTER TABLE USER_ROLES ADD CONSTRAINT FK_ROLES FOREIGN KEY (ROLE_ID) REFERENCES ROLES (ID) ON DELETE CASCADE ON UPDATE CASCADE;
ALTER TABLE DEPARTMENT
  ADD CONSTRAINT FK_DEPARTMENT_HEAD FOREIGN KEY (HEAD) REFERENCES DEPARTMENT (ID) ON DELETE CASCADE ON UPDATE CASCADE;
ALTER TABLE EMPLOYEE
  ADD CONSTRAINT FK_EMPLOYEE_PERSON FOREIGN KEY (PERSON_ID) REFERENCES PERSON (ID) ON DELETE CASCADE ON UPDATE CASCADE;
ALTER TABLE EMPLOYEE
  ADD CONSTRAINT FK_EMPLOYEE_PROFESSION FOREIGN KEY (PROFESSION_ID) REFERENCES PROFESSION (ID) ON DELETE CASCADE ON UPDATE CASCADE;
ALTER TABLE EMPLOYEE
  ADD CONSTRAINT FK_EMPLOYEE_DEPARTMENT FOREIGN KEY (DEPARTMENT_ID) REFERENCES DEPARTMENT (ID) ON DELETE CASCADE ON UPDATE CASCADE;
